# Captain's log — 2026-05-23

## Where we are this morning

Branch `integration/from-180bbb5f`.  Carried over from 22-May: the
WhyCon detection chain inside closed-loop flight (s187 / s190 / s191)
was producing under-counts and bimodal PnP — but those tests bundle
perception + EKF + PnP + RPYT→HL handoff + control all together, so
the failure cannot be cleanly attributed.  Per yesterday's diary the
last grindable hypothesis was a detector-side cv2-style threshold
gap (commits `60e7a62f`, `6381b7e7` on T13 iter-21b/21d).

While I was off the loop, a helper landed a much more useful body
of work: an **air-gapped synthetic perception bench** under `todo/`
and `dataset/`.  This is the first time WhyCon is testable as a
component, not as a flight outcome.

## What the helper actually delivered

**Specs (`todo/`)** — A/B convention: A = objective / spec, B =
implementation log.  Author kept A1/A2 stable and amended B1/B2 as
the work landed, which matches `[[feedback-experiments-in-own-folder-
log-dead-ends]]`.

- `TD-S10-A1` — synthetic dataset spec (320×240 grayscale P5 PGM,
  manifest with projected centers/bboxes/visibility classes, mixed
  grid+random sampling, asymmetric 7-marker pad to break pose
  symmetry, visibility=`visible` vs `partial_crop` separated).
- `TD-S10-A2` — validation spec (frame-by-frame OpenCV ↔ sentai_sim
  ablation, manifest as ground truth, paper-backed WhyCon family as
  reference, 16+ report tables, reprojection-gated pose stats,
  pose-ambiguity diagnostic).
- `TD-S10-B1` / `TD-S10-B2` — implementation logs with every iter
  recorded (dead-ends kept).

**Dataset generator** —
`sim/scripts/generate_whycon_synthetic_dataset.py`.

- Camera-only world derived from `sentai_whycon_small.sdf`.  GUI
  active per `[[feedback-gazebo-gui-required]]`.  Run-local copy
  under `dataset/TD-S10-B1/<run>/_work/` so the upstream CrazySim
  world is untouched (per `external_patches/` discipline).
- `gz model -m dataset_camera -p` to resolve entity id, then pose
  request with explicit `id:` (the original `set_pose` by name
  silently no-op'd — first iteration bug, found and fixed).
- 7-marker asymmetric layout: standard 6 + `N=(0.02, 0.10, 0.005)`,
  injected only in the run-local world.

11 runs under `dataset/TD-S10-B1/`, building up from 3-frame smoke
through 30, 100, and finally 368 frames spanning z∈{0.30, 0.50,
0.75, 1.00 m}, yaw 0–315° in 45° bins, mild roll/pitch.

**OpenCV validator** —
`sim/scripts/validate_whycon_synthetic_dataset.py`.

- Two named OpenCV variants: `paper` (literature-backed connected-
  component + concentric ring/dot, Krajník 2013/2014, Nitsche 2015,
  `lrse/whycon`) and `edge_partial` (cropped-marker recovery via
  `minEnclosingCircle` on visible arc, kept labeled as a variant —
  not folded into the parity baseline).
- Centroid match threshold `max(5 px, 0.25·r_outer)`, manifest as
  ground truth, no detector-order assumption.
- 16 report tables under `report_tables/`, including
  `backend_detection_summary`, `backend_geometry_summary`,
  `backend_pose_vs_ground_truth`, `opencv_vs_sentai_ablation`,
  `pose_ambiguity_candidates`, failure-mode by z×n_markers.
- Reprojection-gated pose (`pose_max_reprojection_rmse_px = 0.5`)
  so bad PnP branches don't pollute X/Y/Z statistics.
- IPPE + full correspondence-permutation ambiguity diagnostic.

**sentai_sim REPL runner** —
`sim/scripts/run_sentai_sim_whycon_dataset.py`.

- Symlinks the B1 dataset into `build-sim/sentai_fs_root/` and
  drives `sentai.markers.init("whycon")` +
  `sentai.markers.detect_pgm("/td_s10_b1_dataset/frames/...")`
  through the REPL of a running `build-sim/sim/sentai_sim`.
- Reads marker world coordinates from `config.json` and calls
  `sentai.markers.set_marker_world(...)` — no hardcoded 6-marker
  layout.

**C++ port + MicroPython binding**
(`M` in git status, uncommitted):

- `sentai_aruco.cc` rewritten with OpenCV-parity WhyCon: global
  inverse threshold sweep (100/130/150), 8-CC, outer circular gate,
  concentric dark-dot pairing via bbox/moment centre, center-offset
  + dot/outer ratio gates, dedupe.  Component cap raised 96→240
  (Gazebo floor texture creates many spurious small CCs at high z).
- `sentai_markers.cc/h`: new `SentaiMarkersDetection` ABI exposes
  `cx`, `cy`, `axis_a`, `axis_b`, `angle_rad`, `radius_outer`,
  `tvec`, `rvec`, `reproj_err`, `pose_valid`.  New entry points
  `sentai_markers_detect_pgm`, `sentai_markers_get_detection`,
  `sentai_markers_set_marker_world`.
- `bindings/modsentai_markers.c`: MicroPython exposure
  (`detect_pgm`, `get_detection`, `get_detection_tuple`).  QSTRs
  regenerated per `[[qstr-regen]]`.
- W3 concentric relaxation removed.  Helper consciously chose
  algorithmic parity over recall-chasing — exactly the right call
  per A2 §"Non-goals".

## Headline numbers — 368-frame ablation

Run: `dataset/TD-S10-B2/whycon_gazebo_synth_20260523_133537/
validation_20260523_141003/`.

**Detection (fully-visible markers only):**

| backend | recall | FP/frame | complete-frame | centroid p95 |
|---|---|---|---|---|
| opencv  paper | 1.0000 | 0.005 | 0.995 | 1.51 px |
| sentai_sim    | 0.9982 | 0.052 | 0.938 | 1.51 px |

OpenCV–sentai centroid Δp95 **0.36 px**.  This is sub-pixel
algorithmic parity on Gazebo-rendered markers.

**Pose vs ground truth (m, deg):**

| backend | pose frames | tr RMSE | tr p95 | yaw MAE | yaw p95 |
|---|---|---|---|---|---|
| opencv     | 364 | 0.128 | 0.041 | 0.20° | 0.22° |
| sentai_sim | 367 | **0.016** | 0.029 | 0.09° | 0.22° |

OpenCV RMSE is inflated by ~6 mirrored-branch 1 m pose outliers on
4-marker weak-geometry frames at z=1.00 m.  Reprojection gate
catches some, not all — for some, the bad branch has reprojection
RMSE *lower* than 0.5 px.  sentai_sim is more robust on this batch
because the registered marker layout gives it a prior-guided
correspondence (its `get_drone_pose_tuple` does internal
permutation + Kabsch + yaw-anchor mirror picker per
`[[feedback-yaw-anchor-mirror-picker]]`), whereas the OpenCV
ablation path runs exhaustive permutations and is more vulnerable
to mirror branches.

## My viewpoint

1. **This is a real step forward.**  We can now publish
   "WhyCon centroid p95 = 1.51 px, pose RMSE = 16 mm on a 368-frame
   synthetic Gazebo dataset" with ablation-grade backing.  Before
   this work the only WhyCon metric was a closed-loop drift number,
   which bundled detector + EKF + PnP + control + CRTP + Gazebo
   physics into a single failure.  That bundle is now decomposable.

2. **The 22-May "detector recall bug" hypothesis was right.**  T13
   iter-21d (bit-perfect threshold + RETR_EXTERNAL gap) was on the
   correct trail.  The helper extended it correctly: the under-count
   was *primarily* an OpenCV-vs-embedded contour-vs-CC semantic
   mismatch, not a threshold tuning issue.  Resolution: drop W3,
   move both implementations to the same CC + concentric ring/dot
   structure, validate visually first, claim parity only after
   recall numbers AND geometry numbers (centroid + radius + axes)
   agree.  Good methodology.

3. **Pose ambiguity is the real next problem, not detector recall.**
   On a 6-marker symmetric layout 45 / 75 pose frames had a
   near-ambiguous IPPE/permutation alternative.  The asymmetric 7th
   marker helps but does not eliminate it — any frame where only a
   weak 4-marker subset is visible can still mirror-branch.  This
   is consistent with the closed-loop "PnP bimodal" symptom from
   s191 iter-15..18.  The validator now exposes ambiguity as a
   diagnostic, but flight code needs prior-guided assignment (prev
   pose + yaw/IMU prior + Hungarian on projected NN + residual
   early-rejection).  Spec is already in B2 §"Runtime Pose
   Assignment Options"; we need to fold it into `sentai.markers.
   get_drone_pose_tuple` properly.

4. **sentai_sim FP=19 across 368 frames is the next focused
   debugging target.**  False positives can mask misses if a
   spurious detection lands within match-threshold of a true marker.
   Need to inspect: are the 19 FPs ground-texture artefacts at
   z=1.00 m?  Re-tune the small-dot pairing gate, but do it on the
   dataset bench, not in flight.

5. **`edge_partial` is a labeled variant, not the parity baseline.**
   The helper correctly resisted folding cropped-marker recovery
   into default metrics: cropped markers are `partial_crop`
   manifest stress cases, not the parity ground truth.  Keeping
   `edge_partial` as a separate named OpenCV variant is the right
   call.  If we want cropped-marker recall in flight, port
   `edge_partial` to sentai_sim *as a labeled variant* and rerun
   ablation — don't bolt it into the production path silently.

6. **Anti-cheat status: clean.**  Sentai consumes only camera
   frames (320×240 grayscale PGM = same encoding the embedded
   pipeline gets from `gz_to_uds_bridge`); manifest ground truth is
   used only host-side in the validator, not piped into sentai_sim.
   Per `[[feedback-sentai-sim-air-gapped-from-truth]]` this is by-
   the-book.  Contrast with yesterday's `SENSOR_TOF_SIM` plugin
   patch (now backed out per the 22-May plan).

7. **Uncommitted work needs landing.**  `git status` shows the C++
   detector rewrite, the new ABI, the binding, the QSTR diff, and
   sim build-version bump are all still uncommitted.  Build #558+
   is what produced the 368-frame numbers above, so the binary
   exists but the source isn't recorded yet.  Risk: another grind
   session on s191 will rebuild from the last committed state
   (6381b7e7) and lose the parity port.  Need to commit
   under a fresh task code before doing anything else.

## Plan from here

1. Read git diff in full, commit the WhyCon parity port + ABI +
   binding + QSTR under a new task code (probably
   `OP-S10-W21-T14: WhyCon OpenCV-parity port + synth dataset
   bench`).  WBS-auditor before commit.
2. Inspect the 19 sentai_sim false positives on the 368-frame run
   and the 4 missed markers at z=1.00 m.  Use the existing
   `sentai_detection_contact_sheet.png` + `sentai_detected_overlays/`
   as starting artefacts.
3. Fold prior-guided correspondence into
   `sentai_markers_get_drone_pose` so flight code stops mirror-
   branching on weak 4-marker frames.  Use the offline validator
   as the regression bench for this — every code change reruns the
   368-frame ablation and the headline pose RMSE / yaw p95 numbers
   should not regress.
4. Re-run s191 (`OP-S10-W21-T13`) against the new committed
   detector.  Open question: does T13 iter-21d's "33/44 match
   (75%)" go to ~98% now?  If yes, T13 closes.  If not, what
   else is different in flight vs synthetic (lighting / motion
   blur / CrazySim camera bridge vs Gazebo-direct)?
5. Promote dataset bench to ARM target: same PGM frames through
   the embedded build, verify byte-identical detector output.
   This is the next `[[sim-arm-parity-check]]` opportunity.

## Anti-cheat audit

Ran mentally against `[[feedback-sentai-sim-air-gapped-from-
truth]]`:

- Dataset frames are camera-only renders (Gazebo `dataset_camera`
  publishing `/dataset_cam/image`).  No GT topics fed into
  sentai_sim.
- Manifest contains GT but is consumed only by host-side
  validator.  sentai_sim REPL runner passes only PGM bytes and
  marker world coordinates (those are config, not GT — same set
  exists on real HW from calibration).
- `set_marker_world(...)` does receive the world coordinates of
  the 7 markers, but these are the *map* (known pad layout), not
  the *camera/drone pose ground truth*.  Distinction matches the
  rule.

Clean.

## Open questions

- Should we mirror this dataset bench under `experiments/sNNN/`
  per `[[feedback-no-tmp-experiments]]`, or is the
  `dataset/TD-S10-B*/` layout sufficient as a sibling artefact
  type?  Helper chose the latter; I think that's fine because
  these are *datasets*, not single-shot experiments, but worth
  recording the precedent.
- Where in `ideas/objects_plan/` does this work attach?  Probably
  under W17 (WhyCon) or a new W21-sibling for "perception
  bench".  Need to file the per-WP spec per
  `[[project-objects-plan-per-wp-specs]]`.
- TD- task family: is `TD` a new orthogonal axis (Test Dataset)
  alongside `EXP-` and `FW-`?  If so it should be registered in
  `ideas/wbs.md`.  If it's meant to be a WBS-stage code then it
  needs an `OP-S{N}-W{M}-T{K}` mapping.
