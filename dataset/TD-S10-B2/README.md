# TD-S10-B2 Validation Artifacts

Validation outputs for `TD-S10-A2_validate_synthetic_whycon.md`.

Raw synthetic datasets live under:

```text
dataset/TD-S10-B1/
```

Validation runs for those datasets should live under:

```text
dataset/TD-S10-B2/<dataset_id>/validation_YYYYMMDD_HHMMSS/
```

Each `summary.json` should record the source dataset path.
