#!/bin/sh
#
# @name Gazebo x500 SentAI (external vision pos, no GPS) — PX4 v1.14
# @type Quadrotor
#

. ${R}etc/init.d/rc.mc_defaults

PX4_SIMULATOR=${PX4_SIMULATOR:=gz}
PX4_GZ_WORLD=${PX4_GZ_WORLD:=sentai_crazysim}
PX4_SIM_MODEL=${PX4_SIM_MODEL:=x500_sentai}

param set-default SIM_GZ_EN 1

# No GPS sim — vision provides horizontal pos.  Baro stays for altitude.
param set-default SENS_EN_GPSSIM 0
param set-default SENS_EN_BAROSIM 0
param set-default SENS_EN_MAGSIM 1

# Allow arming without GPS.
param set-default COM_ARM_WO_GPS 1

# EKF2 external-vision fusion (v1.14 EKF2_EV_CTRL bit field):
#   bit 0 (1) = horizontal position
#   bit 1 (2) = vertical position
#   bit 2 (4) = velocity
#   bit 3 (8) = yaw
# Horizontal + vertical pos from vision (=3).  Yaw stays from mag.
# Sending vision-yaw triggers EKF2 Yaw estimate error when vision
# frame and mag frame disagree.
param set-default EKF2_EV_CTRL 3
# Disable GPS, optical flow, rangefinder.
param set-default EKF2_GPS_CTRL 0
param set-default EKF2_OF_CTRL  0
param set-default EKF2_RNG_CTRL 0
# Baro for altitude.
param set-default EKF2_BARO_CTRL 1
param set-default EKF2_HGT_REF   0
# Mag in custom multicopter mode.
param set-default EKF2_MAG_TYPE 0
# Pose delay (camera → host detect → MAVLink → EKF, very short for SIM).
param set-default EKF2_EV_DELAY 5

# Rotor geometry — identical to x500 baseline.
param set-default CA_AIRFRAME 0
param set-default CA_ROTOR_COUNT 4

param set-default CA_ROTOR0_PX 0.13
param set-default CA_ROTOR0_PY 0.22
param set-default CA_ROTOR0_KM  0.05

param set-default CA_ROTOR1_PX -0.13
param set-default CA_ROTOR1_PY -0.20
param set-default CA_ROTOR1_KM  0.05

param set-default CA_ROTOR2_PX 0.13
param set-default CA_ROTOR2_PY -0.22
param set-default CA_ROTOR2_KM -0.05

param set-default CA_ROTOR3_PX -0.13
param set-default CA_ROTOR3_PY 0.20
param set-default CA_ROTOR3_KM -0.05

param set-default SIM_GZ_EC_FUNC1 101
param set-default SIM_GZ_EC_FUNC2 102
param set-default SIM_GZ_EC_FUNC3 103
param set-default SIM_GZ_EC_FUNC4 104

param set-default SIM_GZ_EC_MIN1 150
param set-default SIM_GZ_EC_MIN2 150
param set-default SIM_GZ_EC_MIN3 150
param set-default SIM_GZ_EC_MIN4 150

param set-default SIM_GZ_EC_MAX1 1000
param set-default SIM_GZ_EC_MAX2 1000
param set-default SIM_GZ_EC_MAX3 1000
param set-default SIM_GZ_EC_MAX4 1000

param set-default MPC_THR_HOVER 0.60
