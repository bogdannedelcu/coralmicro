# s191 — Calib RPYT-only cascaded PD (OP-S10-W21-T12)
# Iteration log (append-only, English).

## iter-1 (2026-05-22 12:30)  NAV_TIMEOUT — first full run

params: Kp_xy=30 Kd_xy=30 Kp_yaw=150 yaw_target=0
result: phase 1-5 all ran end-to-end!  Drone climbed via PD altitude
to z=0.749, then NAV phase tried to spin yaw to 0 from +1.57 rad
(yaw saturated at -60°/s).  Drone yawed + drifted to (0.13, 0.029,
0.92).  GT confirmed climb to 0.97m.

Key finding: yaw=1.57 at handoff is a FRAME CONVENTION OFFSET in
get_drone_pose_tuple (pad geometry + camera mount), NOT actual cf2
yaw drift.  Locking to 0 makes drone spin.

## iter-2  yaw target = INITIAL yaw

fix: target_yaw = pd_xyz_yaw[3] (snapshot at phase5 start), wrap
angle into [-π, π].
result: yaw stayed near 1.57-1.62 (stable lock to initial) BUT drone
still overshoot, crashed off-pad.

## iter-3  add world→body frame rotation

fix: rotate world err by -yaw before PD, applied "FLU pitch/roll"
sign conventions.
result: MUCH WORSE — drone diverged to (-1.18, +0.68, 0.015).
yaw flipped ±1.5 across frames (Kabsch mirror picker chaos).
World→body rotation amplifies any yaw error → divergence.

## iter-4  lower gains 30→10, no body rotation

fix: KP_XY=10, KD_XY=10, KP_YAW=20.
result: drone went to (0.76, 1.06, 0.011), GT peak 5.14m!  yaw_rate
saturated at +60°/s because yaw flipped 1.57→-1.71 → false 3 rad err.

## iter-5  yaw_rate=0 (disabled)

fix: yaw_rate command = 0 regardless of PnP yaw.
result: drone STILL drifted to (-0.20, -1.42, 0.015).  Roll/pitch
PD with unknown sign convention destabilized.  cmd reached ±35° at
peak — saturating, oscillating.

## iter-6  Z PD only, roll=pitch=yaw=0

fix: disabled XY PD entirely.  Only Z PD via thrust.
result: drone STILL drifted — but slower.  Climbed to 1.10m
(Z PD reduced thrust to 30k but drone had upward momentum from
phase4 transition that Kd couldn't damp fast enough).  Drifted
xy because cf2 stabilizer holds ATTITUDE not POSITION — with
roll=pitch=0 drone is level but residual velocity from phase4
continues unchecked.  Eventually crashed at (-1.42, -0.36).

Key observations:
- vxyz at t=15: (-0.20, +0.51, +0.17) — significant momentum
- Sim has minimal drag — drone glides
- WITHOUT active XY damping, drone drifts indefinitely

## What we LEARNED across iter-1..6

1. **PnP yaw is unreliable** — flips between mirror solutions per
   frame.  Cannot use for yaw lock or for body→world rotation.
   Future: use cf2 gyro yaw (sentai.crazy.pose()[3]) OR add hysteresis.

2. **Axis identification is REQUIRED** — pitch/roll → X/Y mapping
   is NOT identity.  May be swapped, may have wrong sign.
   Calibration must DETERMINE this in-flight via test pulses.

3. **Gentle gains AT FIRST** — operator-stated: "miscari foarte mici"
   for axis ID, otherwise destabilize.  Recovery requires OPPOSITE
   pulse to return drone to start.

4. **Phase4 → Phase5 transition has residual velocity** — drone
   carries momentum into XY hold.  Need to damp velocity BEFORE
   testing axis sign.

## Open: iter-7+ approach (NOT IMPLEMENTED YET)

State machine:
  5a. Z PD + roll=pitch=0 for ~2s → let drone drift naturally,
      sample (x_drift_y_drift) baseline.
  5b. Tiny pitch test pulse (+1° for 0.3s) + Z PD → measure dx, dy
      vs baseline.  Apply opposite pulse to return.
  5c. Tiny roll test pulse → measure dx, dy.  Opposite.
  5d. Build 2x2 mapping matrix [pitch_x roll_x; pitch_y roll_y]
  5e. Invert mapping → from desired (dx, dy) compute required
      (pitch, roll).
  5f. Apply XY PD with LEARNED axis map.

Risk: drift during pulse may swamp the test signal.  Need pulse
magnitude > drift over pulse duration.

Test pulse: pitch=+1° for 0.3s → drone accel ≈ g·sin(1°) = 0.17 m/s²
→ over 0.3s velocity grows to 0.05 m/s, displacement 8mm.  Drift
of 5cm in 2s = 0.025 m/s → during 0.3s = 7.5mm.  Signal:Noise ≈ 1:1.
TINY margin.  May need larger pulse (+2°) or longer (0.5s).

Need to commit progress + continue in next session.

## iter-7..9  Strategy A: hover() after EKF convergence

iter-7  added EKF convergence detection (cf2.pose vs PnP).
iter-8  relaxed tol 5→10cm, ticks 10→5.
iter-9  PnP-miss tolerance (don't reset streak on n<4).
        → converged in 21 ticks (700ms).  hover() ran 5s.
        Max excursion 22cm.  Status HOVER_UNSTABLE due to threshold.
        OPERATOR CONFIRM: switch happened, drona stable cu oscilații.

## iter-10..12  added phase 7 cross sweep + return-to-origin

iter-10/11 EKF convergence FLAKY across runs (iter-10 fail, iter-11
        pass with 0.65m excursion in hover).  LPF removed per operator.

iter-12  position setpoint replacing hover() velocity pulses (closed
        loop instead of open-loop resonance).

## iter-13  FRAME MISMATCH discovered (critical finding)

params: position setpoint world frame at (0,0,z)

result: drone DRIFTS in -Y per PnP, ends up +Y per GT.  **PnP frame
≠ Gazebo world frame**.  cf2 navigating in PnP frame which is flipped
vs Gazebo.  Drone goes wrong direction physically.

KEY INSIGHT: this IS the calibration problem.  R_cam_to_body in
sentai.calib is exactly the frame offset between vision PnP and
body/world.  Using cf2 position setpoint with PnP-fed ExtPos hides
the problem at a higher level but doesn't solve it.

For axis ID:
- Can't use position setpoint (depends on frame which we're calibrating)
- Must use hover() body-frame velocity pulses + Kabsch on world response
- This IS what we tried in iter-7 axis_id branch — should revisit

NEXT (deferred): rewrite phase 5 with hover()-based axis ID:
1. hover(0,0,0,z) to settle drone in body frame
2. hover(+v, 0, 0, z) pulse for 0.3s + opposite recovery → measure
   (dx_w, dy_w) world response per pitch+ body command
3. hover(0, +v, 0, z) pulse + recovery → (dx_w, dy_w) per roll+
4. Build 2×2 axis map → R_body_to_world
5. After axis ID, commit_R + sweep using world frame INTERPRETED via R

## iter-14 (2026-05-22 16:30)  AXIS ID PULSE MEASURED — first concrete data

reverted from position setpoint to hover() body-frame pulses (per
operator's frame-mismatch insight).

Approach:
- Phase 5a settle: hover(0,0,0,z) for 2s → baseline samples
- Phase 5b pulse: hover(+V_body, 0, 0, z) for 0.8s → sample after
- Recovery: hover(-V_body, 0, 0, z)
- Repeat for +Y body

Results:
- baseline median (after settle): (+0.057, -0.018, +0.766)
- after +X body pulse: (-0.021, -0.051, +0.759)
- DELTA: (-0.078, -0.033, -0.007)
  → body +X command produces WORLD displacement (-0.078, -0.033)
  → normalized direction: (-0.92, -0.39)
  → inferred drone yaw in PnP frame = atan2(-0.033, -0.078) ≈ -157°
- +Y body pulse: FAILED (few samples) — drone drifted off pad between
  pulses, markers lost.  Need stronger return-to-origin between
  pulses.

GT validation: drone went to +1.46m X in Gazebo while PnP showed -X.
Confirms 180°-rotated PnP frame vs Gazebo.

KEY INSIGHT: this pulse measurement IS the calibration!  The vector
(-0.078, -0.033) per +X body pulse tells us the drone yaw in PnP
frame.  Once we know R, we can:
- Rotate PnP coords by +R before sending ExtPos
- cf2 Kalman now operates in body/Gazebo frame consistently
- Position setpoints work correctly

Next iter (15): use iter-14's measured R to FIX ExtPos feeding —
drone should hold position.  Then re-pulse +Y to get second column
of R.  Then Kabsch refines.

## iter-15..18 (2026-05-22 16:30-18:00)  AXIS ID — DEEPER ISSUE FOUND

Iter-15: hover-based local-baseline pulses → CATASTROPHIC divergence
(drone climbed to 25m).  Root cause: hover(0,0,0,z) uses cf2 Kalman
position, which is fed by ExtPos in PnP frame.  Frame mismatch → cf2
commands wrong direction → drone drifts off pad → no markers → Kalman
Z diverges → uncontrolled climb.  **NEVER use hover() while PnP frame
unknown.**

Iter-16: pivot to RPYT pitch/roll degree pulses (cf2 attitude PID only,
no Kalman position dependency).  Z PD altitude lock retained.
Result: axis_id_ok=True but x1/y1 contaminated by `_try_pnp` returning
n>=4 with pose=None sentinel (-1,-1).  M_inv garbage.

Iter-17: sample-window fix (median over 0.4s pre/post, filter
sentinels).  Pitch+ produced clean (-0.106, -0.067) m in world frame
(drift-subtracted).  Roll+ produced ONLY (0.021, -0.007) m — same
magnitude as baseline drift.  M det = 8.6e-5 → singular.

Iter-18: pulse 5°→10° for better SNR.  Baseline measured vx_drift =
-25 cm/s, vy_drift = -12 cm/s.  BUT GT shows drone XY range only
1 cm during baseline.  Conclusion: **PnP itself is bimodal** —
yaw_anchor_mirror_picker flips between mirror solutions on
near-symmetric WhyCon pad, producing 12cm jumps with stationary
drone.

KEY FINDING: axis ID requires STABLE PnP first.  Mirror picker is the
upstream problem.  Either:
- Disable mirror picker during axis ID phase (commit to one mirror)
- Use cf2 gyro yaw as the picker disambiguator
- Add hysteresis on picker — once chosen, stick unless yaw rotates
  > 45°
- Use ONLY canonical 4-marker subset to avoid bimodal subset choice

DRONE BEHAVIOR: even with axis_id_failed, drone stays bounded on pad
and lands within 5cm of origin (FINAL (-0.029, +0.043) iter-18).
[[sim-test-must-return-home]] is satisfied — that's a real win.
Phase 1-4 takeoff is rock-solid; phase 5 axis ID is the open problem.

Next iter direction: fix PnP stability before re-attempting axis ID.
Likely need to investigate yaw_anchor_mirror_picker behavior in s191's
hover/pad geometry.
